
  # Mountain West

  This is a code bundle for Mountain West. The original project is available at https://www.figma.com/design/btakGdjuR7c7EQt9oTUTod/Mountain-West.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  